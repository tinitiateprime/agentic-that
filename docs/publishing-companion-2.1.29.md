# AgenticThat Companion 2.1.29

This release recognizes YouTube Studio's current visible `Video processing`
dialog by its exact accessible dialog identifier. YouTube's outer custom host
reports zero size, so Companion now observes the visible inner dialog directly
and closes the managed browser immediately.

All other publishing behavior is unchanged.
