# Facebook Publisher

The live Facebook browser publisher is implemented in `../queue-runner/server/services/publishers/facebook.ts` and orchestrated by the shared Publish Queue Runner.

Planned service type: publishing.
