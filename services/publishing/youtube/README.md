# YouTube Publisher

The live YouTube browser publisher is implemented in `../queue-runner/server/services/publishers/youtube.ts` and orchestrated by the shared Publish Queue Runner.

Planned service type: publishing.
